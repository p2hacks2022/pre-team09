/*
メソッド：信用度を変更する
引数：信用度(float), 予定日時ArrayList、締切ArrayList、推測ArrayList,終了ArrayList, 完了ラベル配列(int...0 or 1)
パラメータ：
alpha_prePlan(予定前に完了したときのパラメータ)
alpha_postPlan (予定後・推測前に完了したときのパラメータ)
beta_preDeadline(推測後・締切前に完了したときのパラメータ)
beta_postDeadline (締切を超過したときのパラメータ)
*/
/*
void updateConfidence(float degreeOfConfidence, ArrayList<Calendar>  planDateArray ,ArrayList<Calendar> deadlineDateArray, ArrayList<Calendar> predictDateArray, ArrayList<Calendar> finishDateArray,int isDone[]){
  Calendar Now;
  Now = Calendar.getInstance();
  for(int i=0; i < planDateArray.size(); i++){
    float incliment;
    if(
    
    degreeOfConfidence += 
  }
}
*/
